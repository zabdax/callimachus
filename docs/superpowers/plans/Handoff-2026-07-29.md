# Handoff Log — HSC Study Tracker Implementation Plan

> **Audience:** The next agent that will write the v1.0 implementation plan.
> **Author (previous agent):** ZCode (MiniMax-M3) — failed to deliver the full plan; capturing state so the next attempt does not re-do work.
> **Date:** 2026-07-29
> **Workspace:** `F:\Studytracker\` (Windows, Git Bash, no `apps/web/` scaffold yet).

---

## 1. TL;DR for the next agent

- The **design spec is done, reviewed, and committed** (`f5f8a18`). Do **not** rewrite it.
- A **plan index README** exists at `docs/superpowers/plans/2026-07-29-README.md` and **names three sub-plans that have NOT been written yet**.
- Your job is to **write those three sub-plan files** (and only those). Each is a detailed, TDD-style, task-by-task plan that the subagent-driven-development / executing-plans skills can pick up.
- The previous failure mode was trying to plan everything in a single mega-document. **Do not do that.** Write three separate files, each one bounded.

| Plan index expects (does NOT exist yet) | What you must produce |
|---|---|
| `2026-07-29-foundation-and-profile-plan.md` | M1 + M2 + M3 |
| `2026-07-29-timer-and-progress-plan.md` | M4 + M5 |
| `2026-07-29-subscription-admin-and-ship-plan.md` | M6 + M7 + M8 |

---

## 2. What was already accomplished this session

### 2.1 Committed to git
- `f5f8a18 docs: design spec v1 for HSC study tracker`
  - 660-line file at `docs/superpowers/specs/2026-07-29-hsc-study-tracker-design.md`. **Read this first.** It is the source of truth for every decision below; do not contradict it without flagging.

### 2.2 Uncommitted artifacts on disk (do not lose)
- `docs/superpowers/plans/2026-07-29-README.md` — plan index that links to the three missing sub-plans. **Preserve its `Tech stack` and `Quality bars` tables verbatim** when writing the sub-plans; the sub-plans should reference back to this index for stack choices rather than re-declaring them.
- `Study-tracker-copy/` — a Puppeteer scrape of the live reference site `study-tracker-hsc.web.app` (committed to local tree, NOT in git). This is **research material only**:
  - `Study-tracker-copy/index.html` — the reference landing/dashboard markup
  - `Study-tracker-copy/study-tracker-hsc.web.app/js/` — `access.js`, `dashboard.js`, `firebase-config.js`, `syllabus-data.js`, `syllabus.js`, `timer.js`, `ui-helpers.js`
  - `Study-tracker-copy/subscribe.html` — reference subscription page
  - `Study-tracker-copy/fonts/`, `Study-tracker-copy/cdn.jsdelivr.net/`, `Study-tracker-copy/www.gstatic.com/` — cached fonts/CDN assets
  - Treat as **read-only** reference. Do not import or re-bundle. The design spec explicitly says "clean-room re-implementation; we re-type, not scrape."
- `download2.js` — the Puppeteer scraper script that produced `Study-tracker-copy/`. Listed in the uncommitted tree but not needed for the plan.
- `package.json` / `package-lock.json` / `node_modules/` — only the scraper's dependencies (`puppeteer`, `website-scraper`, `mkdirp`). They are **scraping tooling, not app dependencies**. The real app will be Vite + React + TS and will have its own `apps/web/package.json`.
- `.genome/` — CodeGenome architectural knowledge graph for this repo. `watcher.db` is present, so per `AGENTS.md` you SHOULD use the CodeGenome MCP (`codegenome mcp-start`) for symbol/dependency queries instead of raw `grep`. If MCP tools are not available, fall back to `.genome/graph.json` reads. (In practice for a writing-plans task, MCP graph reads are unlikely to be needed; the plan should be derived from the spec, not the graph.)
- `.cursor/rules/`, `.github/copilot-instructions.md`, `.windsurfrules`, `AGENTS.md` — repo agent-instruction files. They are **not user-supplied product instructions**; they are tooling configuration. Ignore for the planning task.

### 2.3 What was NOT accomplished
- **No `apps/web/` Vite scaffold.** No `src/`. No `package.json` for the app.
- **No Firestore rules file.** No Firebase project created.
- **No Cloud Functions code.** No `functions/` directory.
- **No syllabus data** (the design says we re-type, not scrape — this is a Plan 1 task).
- **No tests.**
- **No CI.**
- **No sub-plan files** — the three `2026-07-29-{foundation,timer,subscription}-and-*-plan.md` files do not exist. **This is what you must create.**

---

## 3. The failure mode of the previous attempt (so you don't repeat it)

The previous agent's instruction was "create the implementation plan." It began by reading the full 660-line spec, then tried to produce one giant plan. The session ended before any sub-plan file was written; only the index README was committed. Likely root causes:

1. **Scope was unbounded.** Writing 15 weeks of TDD tasks for a 660-line spec in one shot is a token-burner and produces a plan that nobody can review.
2. **Index was written before sub-plans.** The README names files that don't exist yet. That is a forward-reference that confused the agent into "pre-declaring" content the sub-plans were supposed to hold.
3. **No checklist per file.** Temptation to dump every milestone into one document.

**Do not repeat.** The plan index already exists and is fine. **Write each of the three sub-plan files one at a time, in order, with a TodoWrite checklist for each file.**

---

## 4. Required reading (in this order)

1. `F:\Studytracker\docs\superpowers\specs\2026-07-29-hsc-study-tracker-design.md` — the spec. Skim §0, §1, §2, §3, §6, §11 first; refer back to other sections as you write each plan.
2. `F:\Studytracker\docs\superpowers\plans\2026-07-29-README.md` — the existing index. It already declares the milestone-to-plan mapping you must follow.
3. `F:\Studytracker\AGENTS.md` — local instruction file. It mandates CodeGenome MCP for codebase queries; the plan-writing task does not require code queries, so this is mostly informational.
4. *(Optional, if you need behavior detail)* `F:\Studytracker\Study-tracker-copy\study-tracker-hsc.web.app\js\timer.js` — original timer's `pauseAndNotify` bug is the *reason* M4 exists; reading this file clarifies the regression test we must write.

---

## 5. Hard constraints from the spec (carry these into every sub-plan)

These are **non-negotiable** because the user has already approved them. The next agent MUST NOT re-open them in the plan without an explicit user message.

### 5.1 Stack (from spec §2.1 and README)
- TypeScript (strict) + React 18 + React Router 6 + Vite
- Tailwind CSS + shadcn/ui + "Cool Slate" palette tokens (spec §6.1)
- Zustand (per-feature) + TanStack Query
- react-hook-form + Zod
- Recharts (RadialBar for pace card) + Framer Motion + Lottie React + Lucide React
- i18next + react-i18next + @formatjs/intl (ICU MessageFormat) — files at `apps/web/src/messages/bn.json` and `en.json`
- Backend: Firebase Auth + Firestore + Cloud Functions Gen 2 + Cloud Storage + FCM
- Testing: Vitest + RTL + Playwright + @firebase/rules-unit-testing
- Lint: ESLint + Prettier + Husky + lint-staged
- CI: GitHub Actions → Firebase Hosting preview channels
- Obs: Sentry (5K events/mo) + Firebase Analytics

### 5.2 Project structure (spec §3.1)
```
apps/
  web/                           # PWA
  functions/                     # Firebase Functions (TS)
  packages/
    capacitor/                   # Android wrapper — DEFERRED to v1.1
```

There is **no `src/` at the repo root.** Plan 1 Task 1 must `mkdir apps/web`, `cd apps/web`, `pnpm create vite@latest . -- --template react-ts` (or `npm`), then move on.

### 5.3 Data model (spec §4)
Firestore collections are spelled out exactly. Every task that touches a collection must name the path and the fields. Sub-plans should include a short "Data model touched in this milestone" section per milestone.

### 5.4 Trust boundaries (spec §5)
- **Server-only writes:** `users.subscription`, `users.trialEnd`, `users/{uid}/sessions/*`, `/analytics/*`, `/admins/*`.
- **Client can write:** `users.syllabus/*`, `users.upcomingTasks/*`, `users.meta/*` (own uid only), `paymentRequests` (own uid only, create-only).
- **Anti-cheat on timer is server-side** (presence nonces, overlap, daily cap, 6h ceiling). Plan 2 must include a Cloud Function task.

### 5.5 Timer invariants (spec §10) — the *single most important* technical rule
- Timer truth source is `Date.now()` deltas, not tick counts.
- `visibilitychange` and `blur` MUST NOT pause the timer.
- Pause is a deliberate user action only.
- A regression test must prove tab-switch keeps counting accurately within ±1s.

### 5.6 Quality bars (from README)
- 80% line coverage on `src/lib/` and `src/features/*`. UI is best-effort.
- 100% of Firestore rules have positive + negative tests.
- All Cloud Functions have at least one happy-path integration test against the Firestore emulator.
- TDD: every task writes the failing test BEFORE implementation.
- Every task ends with a `git commit` — never bundle multiple tasks into one commit.
- Lighthouse Accessibility ≥ 90; axe-core zero ≥-serious issues on every screen.
- No secrets in repo; `.env` + committed `.env.example`; CI injects real keys.

### 5.7 Milestone-to-plan mapping (from README) — DO NOT change
| Plan | Milestones | Goal at end of plan |
|---|---|---|
| 1 — Foundation & Profile | M1 + M2 + M3 | App boots, Google sign-in, onboarding, syllabus map, manual tasks, SR auto-schedules |
| 2 — Timer & Progress | M4 + M5 | Timer persists across tab-switch with ±1s, pace card correct, time-blocking UI, leaderboard reads |
| 3 — Subscription, Admin & Ship | M6 + M7 + M8 | Subs + admin approval, i18n, FCM, data export, a11y/perf audit, Playwright e2e, PWA manifest, privacy policy, marketing page. **v1.0 ships.** |

### 5.8 Milestone content (spec §11)
Each plan file's first section should restate the milestones it owns (1–2 sentences each) and the Definition-of-Done checklist.

### 5.9 Acceptance criteria (spec §16)
The final sub-plan (Plan 3) must include an explicit "v1.0 acceptance criteria" section that copies the 13-item checklist from spec §16 as the Definition of Done for the whole v1.0.

---

## 6. Output requirements for each sub-plan

Each of the three sub-plan files MUST follow the same template. Do not invent a new structure.

```
# <Plan title>

> For agentic workers: REQUIRED SUB-SKILL: superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans.

**Milestones owned:** M? + M?
**Goal of this plan:** <one sentence>
**Depends on:** <previous plan or "nothing">
**Repo root:** F:\Studytracker\

## Overview
<2–4 sentences restating scope.>

## Milestone <N> — <Name>
### Definition of Done
- [ ] <observable, testable thing>
- [ ] ...

### Tasks
For each task:
- [ ] **Task N.M — <verb phrase>**
  - **Files:** <exact paths to create/modify>
  - **Test first:** <write this test, see it fail>
  - **Implement:** <smallest change to pass>
  - **Verify:** <exact command(s) to run, expected output>
  - **Commit:** `<conventional-commit message>`

(...repeat for each task in the milestone...)

## Data model touched in this plan
<copy the relevant /users, /analytics, etc. paths from spec §4>

## Risks specific to this plan
<2–4 bullets with mitigations>

## Definition of Done for this plan
<checklist>
```

**Tone rules for the plan files:**
- One task per `- [ ]` checkbox, not a phase.
- Every task has a **Test first** line. Even for "scaffold" tasks, the test can be "Vitest can resolve a trivial import" — keep the discipline.
- Every task ends with a `git commit` line.
- Prefer `pnpm` if the user's tooling supports it; the spec does not mandate, but Vite scaffolds default to `npm`. Either is acceptable; pick one and stay consistent.

**Size budget per file:**
- Plan 1 (M1+M2+M3): ~300–500 lines.
- Plan 2 (M4+M5): ~250–400 lines.
- Plan 3 (M6+M7+M8): ~300–500 lines.

If you find yourself pushing past 600 lines, you are over-tasking. Cut.

---

## 7. Skeleton for Plan 1 (so you can start writing immediately)

The previous agent did not produce this. Use it as a starting checklist; flesh each task out using spec §11 milestones 1–3 and §3.1, §4, §5.2, §6.1.

### Plan 1 — Foundation & Profile
**Milestones owned:** M1 (Skeleton) + M2 (Auth + profile) + M3 (Syllabus + tasks)
**Depends on:** nothing

- M1 — Skeleton
  - [ ] Task 1.1 — Create `apps/web` Vite + React 18 + TS strict scaffold.
  - [ ] Task 1.2 — Wire Tailwind + shadcn/ui + ESLint + Prettier + Husky + lint-staged.
  - [ ] Task 1.3 — Add "Cool Slate" palette tokens (light + dark) per spec §6.1; add theme class on `<html>`.
  - [ ] Task 1.4 — Add Vitest + RTL + Playwright config; sample "renders a heading" test.
  - [ ] Task 1.5 — Set up GitHub Actions: lint → test → preview deploy (use Firebase Hosting preview channel or a stub for now).
  - [ ] Task 1.6 — Add `.env.example`, `README.md` env section; ensure no secrets in repo.
  - [ ] Task 1.7 — Create empty `apps/functions` Firebase Functions TS scaffold + a trivial `helloWorld` callable + emulator config.
  - [ ] Task 1.8 — Firestore rules: skeleton `match /` blocks that deny everything, with rules-unit-testing harness in `apps/functions/test/rules/`.
- M2 — Auth + profile
  - [ ] Task 2.1 — Firebase Auth Google sign-in (use `signInWithPopup`); `useAuth` hook.
  - [ ] Task 2.2 — `requireAuth` and `requireProfile` route guards (React Router 6).
  - [ ] Task 2.3 — `onboardingProfile` Cloud Function (Auth onCreate): create `/users/{uid}` with `trialEnd = now + 7d` and `timezone: 'Asia/Dhaka'`.
  - [ ] Task 2.4 — Onboarding UI step 1: pick medium (Bangla / English).
  - [ ] Task 2.5 — Onboarding UI step 2: pick batch (HSC-2024 … HSC-2030, public batches only).
  - [ ] Task 2.6 — Onboarding UI step 3: enter college name.
  - [ ] Task 2.7 — Batch CRUD: seed `/batches/HSC-2024 … HSC-2030` documents per spec §9.4.
  - [ ] Task 2.8 — `recomputeBatchStatus` Cloud Function (daily cron 00:00 BST): transitions pre-start / in-session / exam-window / resulted.
  - [ ] Task 2.9 — Firestore rules tests: users cannot write `subscription`, `trialEnd`, `createdAt`; can write their own `college` / `batchId` / `medium`.
- M3 — Syllabus + tasks
  - [ ] Task 3.1 — Re-type `/syllabus/bangla/{subjectId}` for HSC 1st & 2nd year (Bangla medium). **Re-type, do not import from `Study-tracker-copy/syllabus-data.js`.**
  - [ ] Task 3.2 — Re-type `/syllabus/english/{subjectId}` for HSC 1st & 2nd year (English medium).
  - [ ] Task 3.3 — Subject-selection screen (`/app/syllabus`); user picks which subjects they track → write to `users/{uid}/meta/trackedSubjects`.
  - [ ] Task 3.4 — 4-checkbox grid UI per chapter (firstStudy / firstRev / secondRev / thirdRev); writes to `users/{uid}/syllabus/{subjectId}`.
  - [ ] Task 3.5 — Manual "upcoming task" creation UI; list at `/app/tasks`; status `pending | done | skipped`.
  - [ ] Task 3.6 — `scheduledRevisions` Cloud Function (Firestore write-trigger on `syllabus` when `firstStudyDate` is set): create `upcomingTasks` for +7d, +14d, +30d with `source: 'auto-sr'`.
  - [ ] Task 3.7 — Wire task completion back to the syllabus 4-checkbox grid (manual task `done` → flip the corresponding checkbox).
  - [ ] Task 3.8 — Unit tests for SR scheduler dates and for "no duplicate auto-task" idempotency.

*(Stop here. The next agent should fill in the **Files / Test first / Implement / Verify / Commit** detail for each task, then move to Plan 2.)*

---

## 8. Skeleton for Plan 2 (also not produced yet)

### Plan 2 — Timer & Progress
**Milestones owned:** M4 (Timer that doesn't pause) + M5 (Progress + daily plan + leaderboard)
**Depends on:** Plan 1

- M4 — Timer
  - [ ] Task 4.1 — `FocusTimer` component: truth source = `Date.now()` deltas; `startTs` + `pausedAccumMs` in state + `localStorage` + `users/{uid}/activeSession/current`.
  - [ ] Task 4.2 — `visibilitychange` + `blur` handlers: re-render only, never pause. **Regression test:** Vitest + jsdom simulating visibilitychange during a 60s session, assert elapsedMs within ±1s.
  - [ ] Task 4.3 — Pause = deliberate button click or `Ctrl/Cmd+P`. Show "paused" pill.
  - [ ] Task 4.4 — Server-anchored session start: lightweight `sessionStart` callable that records `serverStartTs`; the client uses that as `startTs` to defeat clock drift.
  - [ ] Task 4.5 — `processStudySession` Cloud Function (callable): validate duration, overlap with last session, presence nonces (see spec §5.3), 6h ceiling, daily cap (10 sessions); on success, write `users/{uid}/sessions/{sid}` and update `/analytics/leaderboard_daily/{YYYY-MM-DD}`.
  - [ ] Task 4.6 — Presence-nonce system: server pushes a 60s nonce every 5–15 min; client must echo before deadline; missed nonces count against the session.
  - [ ] Task 4.7 — Offline queue (IndexedDB) for session records; replay on reconnect; idempotency keys.
  - [ ] Task 4.8 — Per-chapter session tagging: subject + chapter dropdown on the timer; session is tagged and rolled up into `users/{uid}/chapterStats/{chapterId}` by `processStudySession`.
  - [ ] Task 4.9 — Integration test for `processStudySession` against the Firestore emulator: happy path, overlap rejected, 6h rejected, nonce-miss rejected.
- M5 — Progress + daily plan + leaderboard
  - [ ] Task 5.1 — `users/{uid}/chapterStats/{chapterId}` schema + roll-up logic.
  - [ ] Task 5.2 — Overview screen `/app`: today's hours, week, streak, exam-countdown widget (4 states per spec §9.5).
  - [ ] Task 5.3 — Pace card with Recharts RadialBarChart; computes `pacePct`, `remainingDays`, `forecastFinishDate` per spec §9.5.
  - [ ] Task 5.4 — Progress screen `/app/progress`: 1st-study %, 1st/2nd/3rd-revision %; today's + week + month hours.
  - [ ] Task 5.5 — `generateDailyPlan` Cloud Function (daily cron 05:00 BST): for each user, build a plan from pending `upcomingTasks` + last-week pace; persist to `users/{uid}/meta/dailyPlan`.
  - [ ] Task 5.6 — Time-blocking UI on Overview: drag blocks onto a day timeline; "N of M blocks completed today" pill.
  - [ ] Task 5.7 — Streak guard: missed-full-day soft warning, never shame.
  - [ ] Task 5.8 — `forecastProgress` callable (spec §8): given uid + batch, return pace forecast.
  - [ ] Task 5.9 — Community leaderboard reads (`/analytics/leaderboard_daily/{date}` and `_monthly/{YYYY-MM}`) with rank-gate of 15 min/day; "no rank" pill if user below gate.
  - [ ] Task 5.10 — `rollUpLeaderboards` Cloud Function (hourly): daily → monthly roll-up; prune `users` map after 30 days.
  - [ ] Task 5.11 — Unit tests: BST midnight split, pace math, overlap detection, access resolver, SR scheduler, batch-status state machine, time-block conflict detection.

---

## 9. Skeleton for Plan 3 (also not produced yet)

### Plan 3 — Subscription, Admin & Ship
**Milestones owned:** M6 (Subscription + admin) + M7 (Differentiators + i18n) + M8 (Polish + ship)
**Depends on:** Plan 2

- M6 — Subscription + admin
  - [ ] Task 6.1 — `generateSignedUploadUrl` callable (Firebase Storage); client uploads screenshot directly to a one-time URL.
  - [ ] Task 6.2 — Plans screen `/app/subscribe`: 7-day trial, ৳50/mo, ৳500/yr; "Pay via bKash" CTA.
  - [ ] Task 6.3 — Payment-request create flow: user enters TrxID + screenshot; writes `/paymentRequests/{reqId}`.
  - [ ] Task 6.4 — Admin gate: `/admins/{uid}` doc with `role`; client `useIsAdmin` hook; route guard for `/admin/*`.
  - [ ] Task 6.5 — Admin approvals queue at `/admin/approvals`: list, screenshot viewer, approve/reject.
  - [ ] Task 6.6 — `approvePayment` Cloud Function (callable, admin): set `users.subscription.status = 'active'`, `expiresAt`, write `audit`, send SMS via SSL Wireless (preferred) or BulkSMS BD.
  - [ ] Task 6.7 — Audit log writes (every admin action) + `/admin/audit` viewer.
  - [ ] Task 6.8 — Admin batch management: CRUD on `/batches/*`; trigger `recomputeBatchStatus` on save.
  - [ ] Task 6.9 — Cohort dashboard `/admin/leaderboards`: per-batch completion %, avg hours, top students.
  - [ ] Task 6.10 — Stripe-free, no-payment-API: this milestone ships with manual review only; bKash Payment API is a v1.1+ upgrade.
- M7 — Differentiators + i18n
  - [ ] Task 7.1 — `i18next` + `react-i18next` + `@formatjs/intl` setup with `messages/bn.json` + `messages/en.json`.
  - [ ] Task 7.2 — Bangla translations of every user-facing string (UI is fully bilingual at v1.0, per user decision).
  - [ ] Task 7.3 — FCM opt-in flow + per-channel preferences (`revisions`, `streakGuard`, `leaderboardOptIn`).
  - [ ] Task 7.4 — FCM token registration on login; store under `users/{uid}.fcmTokens`.
  - [ ] Task 7.5 — Notification senders: revision reminder, streak guard, weekly report (FCM only — WhatsApp digest deferred to v1.1).
  - [ ] Task 7.6 — Data export endpoint (callable): one-click JSON download of the user's data.
  - [ ] Task 7.7 — Account deletion endpoint (callable) + privacy policy page in Bangla.
  - [ ] Task 7.8 — Theme switcher (dark / light / auto) + `prefers-reduced-motion` respect.
  - [ ] Task 7.9 — Streak flame intensity tiers (1–3 / 4–10 / 11–30 / 30+) with distinct Lottie/Framer animation.
- M8 — Polish + ship
  - [ ] Task 8.1 — Accessibility audit: axe-core zero ≥-serious issues on every screen; keyboard-only walkthrough; focus rings.
  - [ ] Task 8.2 — Performance budget: code-split routes, lazy-load chapters, image `loading="lazy"`; Lighthouse ≥ 90 PWA/Perf/A11y on `/app`.
  - [ ] Task 8.3 — Playwright e2e: sign-in → onboarding → first session → first task → see progress; subscribe happy path; subscribe reject path; **timer persistence across tab-switch (60s ±1s)**.
  - [ ] Task 8.4 — Sentry init (browser SDK + tracing); source maps uploaded in CI; alert on spike.
  - [ ] Task 8.5 — PWA manifest + service worker; offline fallback page; install prompt.
  - [ ] Task 8.6 — Marketing landing page at `/`: hero, "what is this", pricing, screenshots, privacy link, "Sign in with Google".
  - [ ] Task 8.7 — Privacy policy + data-deletion flow live BEFORE first paying customer.
  - [ ] Task 8.8 — Final acceptance pass against spec §16 checklist.
  - [ ] Task 8.9 — Tag `v1.0.0` and write release notes.

---

## 10. v1.0 acceptance criteria (copy from spec §16 into Plan 3 as Definition of Done)

The Plan 3 file's "Definition of Done for v1.0" section must restate this checklist verbatim:

- [ ] New student can sign in with Google in <30s.
- [ ] Onboarding asks for medium + batch + college, all required.
- [ ] First focus session saves server-validated and shows in `/app/progress` within 5s.
- [ ] Timer keeps running accurately when the tab is switched or minimized (verified by Playwright with a 60s switch-and-back test, ±1s tolerance).
- [ ] Marking "1st Study" on a chapter auto-creates upcoming revision tasks for +7d, +14d, +30d.
- [ ] Daily Plan widget shows today's planned blocks; completed/incomplete count is accurate.
- [ ] Bangla and English UI both ship complete (no half-translated screens).
- [ ] Palette = "Cool Slate"; no acid-lime / no navy-gold cliche.
- [ ] Lighthouse score ≥ 90 on PWA / Performance / Accessibility on `/app`.
- [ ] Sentry has zero unresolved errors older than 24h.
- [ ] Firestore rules are 100% covered by unit tests.
- [ ] Pace card reflects the correct `batchId` dates; switching batch from settings recomputes within 1s.
- [ ] Privacy policy and data-deletion flow are live before the first paying customer.
- [ ] Admin can approve a payment request and the student sees the green pill without a refresh.

---

## 11. How to use the `superpowers:writing-plans` skill

This is the skill the next agent MUST invoke first (per the using-superpowers rule at the top of the system prompt, plus the AGENTS.md hard rules).

Suggested sequence:
1. `Skill tool → superpowers:writing-plans` (read it; do not skip).
2. `Skill tool → superpowers:brainstorming` if any open questions in spec §15 still need answers (the agent should not invent answers; the user already made the calls: SSL Wireless first, manual review at v1, both BN/EN at v1.0, WhatsApp digest → v1.1).
3. Create a TodoWrite checklist with one item per sub-plan file. Three items. Each item has sub-checkboxes per milestone.
4. Write Plan 1 first. Commit a single commit per milestone-end, e.g. `docs: plan 1 (foundation + profile)`.
5. Repeat for Plan 2, then Plan 3.
6. Update `docs/superpowers/plans/2026-07-29-README.md` only if you change the milestone-to-plan mapping. (You should not.)

**Do NOT:**
- Try to write all three plans in one response.
- Re-declare the stack or palette inside sub-plan files — link to the README and spec instead.
- Open new architectural questions in the plan; the spec is closed for v1.0.

---

## 12. Open risks the next agent should keep in mind

- **Bangladesh board schedule is unverified.** Spec §9.4 calls out that batch dates are placeholder and must be admin-confirmed before launch. Plan 1 Task 2.7 should note this in a comment.
- **WhatsApp digest was punted to v1.1.** Plan 3 must not include WABA tasks; only FCM + email.
- **iOS and Capacitor are out of v1.0.** No `packages/capacitor/` tasks in any plan.
- **Coaching institute B2B plan is v2.** No cohort analytics for paying institutes in v1.0.
- **Mock tests / AI tutor / social features** are explicitly out (spec §14). Do not add tasks for them.
- **Sentry free tier caps at 5K events/mo.** Plan 3 Task 8.4 should set a billing alert at 4K to avoid surprise cutoff.
- **Firebase Spark → Blaze.** Cloud Functions require Blaze. The spec says "no card needed to start" because we don't deploy functions until M4. Plan 1 tasks should be Spark-only. Plan 2 Task 4.5 is the trigger to add a $5/mo billing alert and document the upgrade step in `.env.example` / `README.md`.
- **CodeGenome MCP** is configured via `AGENTS.md` but the plan-writing task doesn't need it. Mention to the user once that after the plans are written and the scaffold lands, they should run `codegenome analyze` to refresh the graph.

---

## 13. File map after the next agent finishes

Target layout when Plan 3 is committed:

```
F:\Studytracker\
├── docs\
│   └── superpowers\
│       ├── specs\
│       │   └── 2026-07-29-hsc-study-tracker-design.md     [exists, committed]
│       └── plans\
│           ├── 2026-07-29-README.md                        [exists]
│           ├── 2026-07-29-foundation-and-profile-plan.md   [next agent writes]
│           ├── 2026-07-29-timer-and-progress-plan.md       [next agent writes]
│           ├── 2026-07-29-subscription-admin-and-ship-plan.md   [next agent writes]
│           └── Handoff-2026-07-29.md                      [this file]
├── Study-tracker-copy\                                    [reference, read-only]
├── .genome\                                               [CodeGenome, do not touch]
├── AGENTS.md, .windsurfrules, .github\                    [agent config, do not touch]
├── package.json, package-lock.json, node_modules\         [scraper tooling only]
└── download2.js                                           [scraper]
```

Once the next agent commits the three sub-plans, v1.0 is "planned" and the actual app build can begin. **No code should be written before the three sub-plan files exist.**

---

## 14. Quick-start prompt for the next agent (copy/paste-ready)

> You are continuing work on the HSC Study Tracker at `F:\Studytracker\`.
> The design spec (`docs/superpowers/specs/2026-07-29-hsc-study-tracker-design.md`) and the plan index (`docs/superpowers/plans/2026-07-29-README.md`) already exist. The previous agent's handoff log is at `docs/superpowers/plans/Handoff-2026-07-29.md` — **read it first**.
> Your job: write the three missing sub-plan files in order, with TDD-style task lists per the template in the handoff. Do not modify the spec, the index, or any code. Do not run app commands. When you finish all three, report back with a one-line summary of total task counts per plan.
> Start with `Skill tool → superpowers:writing-plans`, then `superpowers:brainstorming` only if you find open questions in spec §15.
