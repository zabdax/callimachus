{
  "firestore": { "rules": "firestore.rules" },

  // ---------------------------------------------------------------------
  // ALTERNATIVE-DEPLOY BLOCKS (commented out for v2.0 default).
  // The default v2.0 deploy uses Cloudflare Pages + Workers + R2 (see
  // scripts/deploy-r2.mjs). If you want to go back to Firebase Hosting +
  // Firebase Storage + Cloud Functions instead, uncomment the blocks
  // below and revert the scripts/deploy.mjs entries.
  // ---------------------------------------------------------------------

  // "hosting": {
  //   "public": "apps/web/dist",
  //   "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
  //   "rewrites": [{ "source": "**", "destination": "/index.html" }],
  //   "headers": [
  //     {
  //       "source": "/assets/**",
  //       "headers": [{ "key": "Cache-Control", "value": "public,max-age=31536000,immutable" }]
  //     }
  //   ]
  // },
  // "storage": { "rules": "storage.rules" },
  // "functions": [{ "source": "apps/functions", "codebase": "default", "runtime": "nodejs20" }]
}